import { ButtonDirective } from './button.directive'

describe('ButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonDirective(undefined, undefined)
    expect(directive).toBeTruthy()
  })
})
