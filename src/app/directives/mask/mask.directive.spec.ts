import { MaskDirective } from './mask.directive'

describe('MaskDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskDirective(undefined, undefined)
    expect(directive).toBeTruthy()
  })
})
