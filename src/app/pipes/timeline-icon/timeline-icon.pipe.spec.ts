import { TimelineIconPipe } from './timeline-icon.pipe'

describe('TimelineIconPipe', () => {
  it('create an instance', () => {
    const pipe = new TimelineIconPipe()
    expect(pipe).toBeTruthy()
  })
})
